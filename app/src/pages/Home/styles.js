import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  pageTitle: {
    color: '#000',
    fontWeight: '500',
    fontSize: '18px',
    marginLeft: '18px',
  },

});
