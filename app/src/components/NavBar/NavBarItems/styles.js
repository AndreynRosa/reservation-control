import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  navOptions: {
    display: 'flex',
    alignItems: 'center',
  },
  navIcon: {
    color: '#fff',
  },
  navText: {
    color: '#fff',
  },
});
