import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    width:" 100%",

    display: 'in-line',
  },

  title: {
    fontSize: 14,
  },
  pos: {
    marginBottom: 12,
  },
});
